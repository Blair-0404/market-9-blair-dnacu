mixin input(name, type, placeholder, min, max)
  .input-box
    input(
    class=name
    type=type
    placeholder=placeholder
    minlength=min
    maxlength=max
    )
    //label 입력하신 아이디로 사용이 가능합니다.
